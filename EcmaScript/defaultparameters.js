// function welcome(user, message) {
//     alert(`Hello ${user}, ${message}`);
// }

// welcome("Ryan", "Good morning");
//welcome() -> The function will crush if no input is provided

function welcome(user="Mystery person", message="Goodday") {
    alert(`Hello ${user}, ${message}`);
}

welcome();
